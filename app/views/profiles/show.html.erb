
<div class="container">
    <div class='row'>
                <% @images.each do |image|%>
           <div class="col-md-2">
                <%= link_to image_path(image) do %>
                <img class="img-thumbnail" src=<%= image.img%>  width="500px" height="500px" >
           <% end %>
            </div>    
                <%end%>
          </div>
    </div>
</div>